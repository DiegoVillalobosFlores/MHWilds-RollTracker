{
  "name": "mhwildsartiantracker",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch index.ts",
    "compile:linux": "bun build --compile --target=bun-linux-x64 --minify --bytecode ./index.ts --outfile mhwilds-tracker"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
